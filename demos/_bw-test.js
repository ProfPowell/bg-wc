// Scratch entry for the browser-window comparison test pages (_bw-wrap.html,
// _bw-iframe.html). Loads the site infrastructure + the bg-wc element + the
// browser-window component. Not part of the shipped site; remove with the test
// pages once we settle on an approach.
import 'vanilla-breeze';
import 'vanilla-breeze/css';
import '../src/bg-wc.js';
import '@profpowell/browser-window';
import '../docs/prefer-dark.js';
